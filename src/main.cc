#include <iostream>
#include <string>

#include <stlx.h>
#include <common.h>

using namespace std;

int main()
{
    std::cout << "Hello CMake!" << std::endl;
    std::cout << doublex(3) << std::endl;

    return 0;
}
